# CivicFix — Python Dependencies
# ================================
# Install with:
#   pip install -r requirements.txt
#
# Environment notes:
#   - GPU training requires a CUDA-enabled PyTorch installation.
#     Ultralytics will auto-select GPU if available.
#   - For Kaggle Notebooks, ultralytics is pre-installed; add psycopg2-binary
#     as an additional package if needed.
#   - NEVER install kaggle credentials via pip — use environment variables only.
#
# Minimum Python version: 3.10 (required for union type hints with |)

# ── AI / ML ──────────────────────────────────────────────────────────────────
# Ultralytics YOLOv8 — instance segmentation training and inference
# Version constraint: >=8.0 for YOLOv8n-seg.pt support, <9.0 to avoid
# breaking API changes until explicitly tested.
ultralytics>=8.0.0,<9.0.0

# ── Database ─────────────────────────────────────────────────────────────────
# psycopg2-binary — PostgreSQL adapter for db_integration.py
# Using -binary variant for no-compilation install; for production servers
# consider compiling from source (psycopg2) against the system libpq.
psycopg2-binary>=2.9.9

# ── Image processing ─────────────────────────────────────────────────────────
# Pillow — used by validate_dataset.py for corrupt image detection (PIL.Image.verify)
Pillow>=10.0.0

# ── Configuration ────────────────────────────────────────────────────────────
# PyYAML — used by evaluate_model.py to read args.yaml from training runs
PyYAML>=6.0

# ── Dataset download ─────────────────────────────────────────────────────────
# kaggle — Kaggle CLI used by ingest_datasets.py
# Credentials are read from KAGGLE_USERNAME / KAGGLE_KEY environment variables.
# Never store credentials in source code or commit kaggle.json.
kaggle>=1.6.0

# ── Transitive dependencies (pulled in by ultralytics automatically) ──────────
# torch>=2.0.0         (CUDA or CPU build)
# torchvision>=0.15.0
# numpy>=1.23.0
# opencv-python>=4.6.0
# matplotlib>=3.3.0
# seaborn>=0.11.0
# scipy>=1.4.1
# tqdm>=4.64.0
# requests>=2.23.0
